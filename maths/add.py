def add_nums(num1, num2):
    """The function `add_nums` takes two numbers as input and returns their sum.    
        :param num1: The parameter `num1` is the first number that will be added
        :param num2: The `add_nums` function takes two parameters, `num1` and `num2`, and returns their sum.In the code snippet you provided, `num2` is the second number that will be added to `num1` when thefunction is called
        :return: The function `add_nums` returns the sum of `num1` and `num2`.
    """

    
    answer = num1 + num2
    return answer
