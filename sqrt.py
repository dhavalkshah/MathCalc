import math
def square_root(num1):
    """
    The function `square_root` calculates the square root of a given number.

        :param num1: The `square_root` function takes a single parameter `num1`, which represents the numberfor which you want to calculate the square root
        :return: The function `square_root` is returning the square root of the input number `num1`.
    """
    return math.sqrt(num1)